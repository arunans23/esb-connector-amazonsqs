{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for GetQueueUrl Operation",
  "description": "Output schema for the getQueueUrl operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the getQueueUrl operation.",
      "properties": {
        "QueueUrl": {
          "type": "string",
          "description": "The URL of the queue."
        }
      },
      "required": [
        "QueueUrl"
      ],
      "additionalProperties": false
    },
    "attributes": {
      "type": "object",
      "description": "Metadata about the getQueueUrl operation.",
      "properties": {
        "operationSuccessful": {
          "type": "boolean",
          "description": "Indicates whether the SQS API call was successful."
        },
        "requestId": {
          "type": "string",
          "description": "The AWS request ID for the SQS operation."
        }
      },
      "additionalProperties": false
    }
  },
  "required": [
    "payload",
    "attributes"
  ],
  "additionalProperties": false
}
